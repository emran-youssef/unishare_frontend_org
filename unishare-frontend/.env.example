# Base URL of the backend API (no trailing slash, no /api suffix — that's added by baseQuery.ts)
# Local dev:  http://localhost:8080
# Production: https://<your-backend>.up.railway.app
VITE_API_BASE_URL=
